'use strict';

/**
 * Check if var type if array
 *
 * @version 1.0.1 - 2019-11-28
 * @author Alexandre Pilloud
 *
 * @param {mixed} mixed Var
 * @return {boolean}
 */

export const isArray = (mixed) => {
    return Array.isArray(mixed);
};

export default isArray;
